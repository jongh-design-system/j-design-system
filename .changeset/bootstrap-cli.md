---
"@jongh/cli": major
---

Simplify the CLI around the generated component registry. `init` no longer creates Panda or Tailwind configuration, and the public `guide` and MCP commands are removed. Configure the styling framework in the consuming project and replace any guide or MCP command usage before upgrading.
