---
"@jongh/ui": major
"@jongh/cli": major
---

Rebuild `@jongh/ui` as a token-driven component package with generated ESM, type, shared CSS, and optional theme CSS outputs.

Make `@jongh/cli` initialize copy paths and install components from the generated GitHub registry without configuring a styling framework.
