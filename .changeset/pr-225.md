---
"@jongh/ui": minor
---

Adds a built-in icon system to `@jongh/ui`, including the `Icon` component and seven exported line icons with supported sizing, semantic foreground colors, and standard SVG props. Existing UI components now use these icons internally, and consumers do not need to migrate.
